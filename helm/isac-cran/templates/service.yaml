apiVersion: v1
kind: Service
metadata:
  name: {{ include "isac-cran.fullname" . }}
  labels:
    {{- include "isac-cran.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isac-cran.selectorLabels" . | nindent 4 }}
